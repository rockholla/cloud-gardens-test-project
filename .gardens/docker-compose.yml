---
version: '2'

services:
  web:
    image: "{{ image }}"
    ports:
      - "0:3000"
    labels:
      - "traefik.frontend.rule=Host:{{ subdomain }}.{{ domain }}"
      - "traefik.frontend.entryPoints=http,https"
      - "timestamp={{ timestamp }}"